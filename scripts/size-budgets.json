{
  "//": "Ceilings for the single-file shells. Raise one IN THE COMMIT that needs the bytes, and say what bought them. Checked by scripts/test-spaces-size.mjs.",
  "shells": {
    "spaces": {
      "path": "spaces/dist-single/Bento_Spaces.bento.html",
      "max": 131072,
      "//": "128 KiB. Raised from 125 KiB at 126782 B on 2026-08-05 for the tracker's AGENT surface: fields()/issues()/setField()/newIssue() and six field findings in validate(). Measured cost of that work alone: 2548 B (124234 → 126782), most of it the refusal and finding TEXT, which is the part that makes the surface usable blind and is not worth compressing away. The raise is exactly what this workstream spent, so the headroom the previous entry left for the board work is still there rather than quietly eaten by this. Before: 118 KiB → 125 KiB at 122898 B for the tracker itself (typed fields, the prop and view block types, the board) — an issue tracker in the same file as the notes is the thing neither Linear nor a wiki can do."
    }
  }
}
